/* Game constants */
const GAME_WIDTH = 1100; // Game width in pixels
const GAME_HEIGHT = 550; // Game height in pixel
const GAME_SPEED = 20; // Game speed of the game loop in milisecond
const GAME_ROUND_PER_LEVEL = 80; // Game round per level

/* Enemies constants */
const ENEMY_WIDTH = 60; // In pixels
const ENEMY_HEIGHT = 60; // In pixels
const MAX_ENEMIES = 1;

/* Players contants */
const PLAYER_WIDTH = 80; // In pixels
const PLAYER_HEIGHT = 80; // In pixels

/* Laser constants */
const LASER_WIDTH = PLAYER_WIDTH; // In pixels
const LASER_HEIGHT = 500; // In pixels
const LASER_NUMBER = 5; 

/* Global Elements */
const AppElement = document.getElementById("app"); // App Element
